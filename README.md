# Comparator API

## Info
```bash
Api contains three endpoints:
  >> Create 'left' string  :  PUT /localhost<:port>/v1/diff/<ID>/left
  >> Create 'right' string :  PUT /localhost<:port>/v1/diff/<ID>/right
  >> Compare two strings   :  GET /localhost<:port>/v1/diff/<ID>
